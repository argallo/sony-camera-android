<resources>
    <!-- Base theme. Compose draws the actual UI; this just sets the window
         background and lets the system bars go edge-to-edge. -->
    <style name="Theme.SonyCameraDemo" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/ink</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
